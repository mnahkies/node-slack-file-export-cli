{
  "name": "slack-file-export-cli",
  "version": "0.0.1",
  "description": "cli tool to download files from slack export files",
  "repository": {
    "type": "git",
    "url": "https://github.com/mnahkies/node-slack-file-export-cli"
  },
  "author": "Michael Nahkies",
  "license": "MIT",
  "main": "./dist/slack-file-export-cli.js",
  "scripts": {
    "build": "tsc && tslint --project tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "tslint -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^12.7.1",
    "tslint": "^5.18.0",
    "typescript": "^3.5.3"
  }
}
